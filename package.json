{
  "name": "overbrowsing",
  "version": "1.0.0",
  "description": "Overbrowsing dedicated to fostering a deeper understanding of sustainable web design",
  "type": "module",
  "scripts": {
    "clean": "echo 'Cleaning _site directory...' && rm -rf _site && mkdir _site && mkdir _site/data",
    "copy:assets": "echo 'Copying assets to _site...' && cp -R scripts/graph _site",
    "fetch:data": "echo 'Fetching Carbon info...' && API_DATA=\"$(curl -s 'https://api.websitecarbon.com/site?url=https://overbrowsing.com')\" ; echo \"$API_DATA\" > _site/data/carbon-data.json",
    "minify:css": "echo 'Minifying CSS' && uglifycss style.css > _site/style.css",
    "build:data": "echo 'Building and minimizing API Data. This takes a few seconds...' && node scripts/build/fetchAll.js",
    "build": "yarn clean && yarn copy:assets && yarn fetch:data && yarn minify:css && yarn build:data",
    "serve": "yarn build && echo 'Starting the server...' && node server.cjs"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "nunjucks": "^3.2.4",
    "sharp": "^0.32.6",
    "uglifycss": "^0.0.29"
  }
}
